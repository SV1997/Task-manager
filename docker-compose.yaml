services:
  # mongo:
  #   image: mongo:7
  #   volumes:
  #     - data:/data/db

  api:
    build:
      context: ./backend
    ports:
      - "80:80"
    volumes:
      - logs:/app/logs
      - ./backend:/app
      - /app/node_modules
    env_file:
      - ./environment/backend.env
    # depends_on:
    #   - mongo

volumes:
  # data:
  logs: